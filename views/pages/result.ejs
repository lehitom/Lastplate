<!DOCTYPE html>
<html>
<head>
	<title>Resturant deals</title>
	<link rel="stylesheet" href="week02home.css" type="text/css">
</head>
<body>
<div class="row">
    
	<div class="main">
	<h1>Resturants with deals</h1>
	<p>There are the following restaurants with a deal in zipcode "<%= params.zip %>":</p>
	<p><%= params.sql %> responded with <%= params.id %> with a length of <%= resp.length() %></p>
	<table class="table table-inverse">
      <thead>
        <tr>
          <th>Resturant ID</th>
          <th>Resturant Name</th>

        </tr>
      </thead>

      <tbody>
       <% for(var i=0; i < resp.length; i++){ %>
       <tr>

         <td><%= resp[i].res_id %></td>
         <td><%= resp[i].res_name %></td>

       </tr>
    <% } %>
      </tbody>

    </table>
	</div>
	</div>

	</body>
</html>